<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accommate</title>
</head>
<body>
    <h1>Edit Listing</h1>

    <form action="/listings/<%= listing._id%>?_method=PUT" method="post">
        <div>
            <label for="title">Title : </label>
            <input type="text" name="listing[title]" id="title" value="<%= listing.title %>">
        </div>
        <div>
            <label for="desc">Description : </label>
            <textarea name="listing[desc]" id="desc"><%= listing.desc %></textarea>
        </div>
        <div>
            <label for="location">Location : </label>
            <input type="text" name="listing[location]" id="location" value="<%= listing.location %>">
        </div>
        <div>
            <label for="state">State : </label>
            <input type="text" name="listing[state]" id="state" value="<%= listing.state %>">
        </div>
        <div>
            <label for="price">Price : </label>
            <input type="text" name="listing[price]" id="price" value="<%= listing.price %>">
        </div>
        <div>
            <label for="capacity">Capacity : </label>
            <input type="number" name="listing[capacity]" id="capacity" value="<%= listing.capacity %>">
        </div>
        <div>
            <label for="image">Image URL : </label>
            <input type="text" name="listing[image]" id="image" value="<%= listing.image %>">
        </div>

        <button>Edit</button>
    </form>
</body>
</html>